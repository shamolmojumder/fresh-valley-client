import React from 'react';
import MediaCard from '../MediaCard/MediaCard';

const Home = () => {
    return (
        <div>
          <MediaCard></MediaCard>
        </div>
    );
};

export default Home;