import React, { useEffect, useState } from 'react';
import Tables from '../Tables/Tables';

const ProductList = () => {
 
    return (
        <div>
            {
               // manageProductList.map(mpl=><Tables mpl={mpl}> </Tables>)
            }
        </div>
    );
};

export default ProductList;